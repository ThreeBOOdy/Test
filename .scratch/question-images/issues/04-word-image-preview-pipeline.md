# 04 — 含图 Word 预检链路（解析 → 入库 → 预览）

**What to build:** 教师上传含图 Word 后，预检页逐题显示图片数量与缩略图供核对归属；格式白名单与大小/数量限额校验生效；批次图片仅教师可访问；批内与题库去重按图片内容哈希归一化。

**Blocked by:** 02 — Word 解析器升级：文本与图片联合抽取；03 — Excel 含图整份拒绝

**Status:** completed

- [x] 预检时提取的图片持久化到批次级表，图片 ID 唯一且提交后保持不变（预检期与提交期同一 ID）。
- [x] 预检对白名单（PNG/JPEG/GIF/WebP）之外的格式逐题报错并提示转换；单张超 5MB、每题超 10 张分别报错。
- [x] 批次图片仅教师可访问；预检页对含图行显示「图片 N 张」与缩略图。
- [x] 批内重复与题库重复判定将图片标记按内容哈希归一化：同图不同 ID 视为相同，图片字节不同才视为内容不同。
- [x] 现有 Word/Excel 预检与导入回归通过。
