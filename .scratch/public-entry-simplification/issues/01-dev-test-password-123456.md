# 01 — 开发测试账号密码统一为 123456（种子 + 本地重置）

**What to build:** 开发与 CI 环境中的测试账号 student / teacher / admin 统一使用密码 `123456` 登录；提供本地开发库密码重置命令；端到端测试同步改用新密码，保证开发自测和自动化测试一致。

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

- [ ] 种子初始化在未显式覆盖时默认使用 `123456` 作为测试账号密码。
- [ ] 提供本地开发库重置命令，可把 student / teacher / admin 三个账号密码重置为 `123456`。
- [ ] 本地开发环境配置中的种子密码默认值更新为 `123456`。
- [ ] 所有端到端登录辅助代码使用 `123456`，不再使用旧测试密码。
- [ ] 真实用户的密码策略保持不变，`123456` 仅用于开发/种子账号。
