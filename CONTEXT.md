# 智学练习（zhixue-practice）题库领域

本上下文覆盖题库、题目导入、练习抽题与判分所使用的领域语言。

## Language

**字母类 / 等级（Level）**：
由字母串代码标识的可扩展类题库，如 A、B、C、K、AA；导入阶段不决定字母类，题目先进入公共题池，之后可拉取到一个或多个字母类，练习抽题按字母类过滤。

**知识点类型 / 大类（knowledge point type）**：
独立字典表 `KnowledgePointType`，作为知识点的大类，例如“电工基础”“通信原理”；多 sheet 导入用 sheet 名作为类型，单 sheet / Word 导入由向导选择或新建。
_Avoid_: 知识点分类

**分类号（category code）**：
在某知识点类型下用于生成知识点树的编号/路径（如 `4.1.1`），可搭配知识点名称使用；同一分类号可在不同知识点类型下分别存在。
_Avoid_: 知识点编号、分类

**公共题池（public pool）**：
导入后尚未归入任何字母类的题目集合；题目可稍后在题目管理或归类向导中拉取到字母类。

**归类 / 拉取（assign / pull）**：
把公共题池中的题目关联到一个或多个字母类（创建 `QuestionLevel` 记录）的操作。

**未归类（unclassified）**：
题目当前没有关联任何字母类时的状态。

**知识点名称（knowledge point name）**：
末级知识点的展示名称，未填写时暂用分类号。

**题目编号（external question code）**：
全局唯一业务编号，可空；有值时全局唯一，用于重复检测；无编号时用内容指纹做疑似重复。
_Avoid_: 题号

**题号（question number）**：
Word 文档中标记一道题在文档内位置的编号，支持 `1.`、`1、`、`（1）` 三种格式；仅用于定位，不进入题库。
_Avoid_: 题目编号

**题干（stem）**：
题目的问题文本。

**选项（option）**：
选择题的候选答案，从 A 开始连续编号，选项内容与编号之间用 `、` 或 `.` 分开。

**答案（answer）**：
选择题正确答案的选项编号集合；Word 模板要求“答案”字段必填且不能为空。

**解析（explanation）**：
Word 模板中可选的题目讲解字段；当前系统不保存解析内容。

**嵌入图片（embedded image）**：
Word 文档中嵌入在题干或选项中的图片、公式（公式仅以图片形式存在）；题干与选项均可包含多张，导入时需提取并持久化。
_Avoid_: 配图、插图

**图片标记（image marker）**：
题干或选项文本中代表图片所在位置的占位标记；文本与图片分离存放，渲染时替换为图片。同一图片内容在不同导入中标记不同，重复比对按图片内容而非标记进行。

**题型（question type）**：
当前仅支持 `SINGLE_CHOICE`（单选题）与 `MULTIPLE_CHOICE`（多选题）；判断题、填空题、简答题等暂不支持。

**不定项选择题（indeterminate multiple choice）**：
答案个数不确定的多选题，Word 模板要求在题号后用 `[不定项选择题]` 标注。

**导入批次（import batch）**：
一次题库文件导入产生的预检与提交记录，包含文件名、统计与逐行数据。

**预检（preflight）**：
上传文件后先解析校验、可逐行查看结果、教师确认后才写入题库的流程。

## 暂不支持的题型与内容

**判断题（judgment question）**：
答案仅为“正确/错误”或“对/错”的题型；当前不支持，Word 导入遇到时逐题报错。

**填空题（fill-in-the-blank）**：
题干中用 `（）` 表示空、多个空答案用 `|` 分隔的题型；当前不支持。

**简答题（short-answer question）**：
没有选项、依靠文字作答的题型（含论述题）；当前不支持。

**材料题（material question）**：
用 `[材料题]` 开始、`[材料题结束]` 结束、内部含 `（1）` 等小题的题型；当前不支持，且未来也不计划支持。
